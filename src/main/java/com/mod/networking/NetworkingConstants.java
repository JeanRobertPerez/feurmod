package com.mod.networking;

import com.mod.FeurMod;
import net.minecraft.util.Identifier;

public class NetworkingConstants
{
    public static final Identifier BASIC_PARTICLES_MESSAGE = Identifier.of(FeurMod.MOD_ID, "basic_render_message");

    public static final Identifier SPECIAL_USE_ITEM_MESSAGE = Identifier.of(FeurMod.MOD_ID, "special_use_item_message");
}
